---
import { siteConfig } from "../config";

const hasMainResearch = siteConfig.projects && siteConfig.projects.length > 0;
const hasOtherResearch = siteConfig.projects && siteConfig.projects.length > 0;
const hasAdditionalResearch = siteConfig.experience && siteConfig.experience.length > 0;
const hasRecentActivities = siteConfig.education && siteConfig.education.length > 0;
---

<header
  id="header"
  class="fixed top-0 left-0 right-0 z-50 hidden md:block transition-all duration-300"
>
  <nav class="max-w-7xl mx-auto px-8 py-4">
    <ul class="flex items-center gap-8 justify-center">
      <li>
        <a
          href="#about"
          class="text-gray-700 hover:text-black transition-colors font-medium"
        >
          About
        </a>
      </li>
      {
        hasMainResearch && (
          <li>
            <a
              href="#projects"
              class="text-gray-700 hover:text-black transition-colors font-medium"
            >
              Main Research
            </a>
          </li>
        )
        hasOtherResearch && (
          <li>
            <a
              href="#other-projects"
              class="text-gray-700 hover:text-black transition-colors font-medium"
            >
              Other Research
            </a>
          </li>
        )
      }
      {
        hasAdditionalResearch && (
          <li>
            <a
              href="#experience"
              class="text-gray-700 hover:text-black transition-colors font-medium"
            >
              Additional Research
            </a>
          </li>
        )
      }
      {
        hasRecentActivities && (
          <li>
            <a
              href="#education"
              class="text-gray-700 hover:text-black transition-colors font-medium"
            >
              Recent Activities
            </a>
          </li>
        )
      }
    </ul>
  </nav>
</header>

<script>
  window.addEventListener("scroll", () => {
    const header = document.getElementById("header");
    if (window.scrollY > 100) {
      header?.classList.add("bg-white/80", "backdrop-blur-sm");
    } else {
      header?.classList.remove("bg-white/80", "backdrop-blur-sm");
    }
  });
</script>

<style>
  html {
    scroll-behavior: smooth;
  }
</style>
